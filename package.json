{
  "name": "interactive-portfolio",
  "version": "1.0.0",
  "description": "An interactive portfolio website with engaging user experience",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --port 1001",
    "build": "parcel build index.html",
    "clean": "rmdir /s /q .cache dist 2>nul || echo Directories not found"
  },
  "dependencies": {
    "gsap": "^3.12.2",
    "scrollmagic": "^2.0.8"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
